# This site moved to https://17xande.dev/teleprompter/ — the 17xande.dev repo
# builds it now. Every path keeps its shape under the new prefix, so old docs
# links and shared screenshot URLs land on the same page.
/*  https://17xande.dev/teleprompter/:splat  301
